<!--pages/fight_result/index.wxml-->
<view class="fight_result b-sizing">
    <view class="result pr df justBet ac">
        <view class="me">
            <view class="header">
                <image src="{{meInfo.avatarUrl}}"></image>
                <view 
                    wx:if="{{fightResut.homeScore>fightResut.awayScore}}"
                    class="crown pa animated fadeInDown ac"
                >
                    <image src="../../assets/images/fight_result/crown.png"></image>
                </view>
            </view>
            <view class="name overeli">{{meInfo.nickName||'我'}}</view>
            <view class="score fw600">{{fightResut.homeScore}}分</view>
            <view class="{{(fightResut.homeScore>=rivalInfo.awayScore?'':'error')+' integral fw600'}}">{{fightResut.homeResult}}</view>
        </view>
        <view class="vs">
            <image src="../../assets/images/fight_result/vs_res.png"></image>
        </view>
        <view class="rival pr">
            <view class="header">
                <image src="{{fightResut.awayHeader}}"></image>
                <view 
                    wx:if="{{fightResut.homeScore<fightResut.awayScore}}" 
                    class="crown pa animated fadeInDown"
                >
                    <image src="../../assets/images/fight_result/crown.png"></image>
                </view>
            </view>
            <view class="name overeli">{{fightResut.awayName||"--"}}</view>
            <view class="score fw600">{{fightResut.awayScore}}分</view>
            <view class="{{(meInfo.homeScore<=fightResut.awayScore?'':'error')+' integral fw600'}}">{{fightResut.awayResult}}</view>
        </view>
        <view wx:if="{{plate}}" class="plate pa animated fadeInDown">
            <image src="{{plate}}"></image>
        </view>
    </view>
    <view class="btn-box df justBet">
        <button hidden="{{!plate}}" type="primary" class="result-btn b-sizing" data-type="regame" bindtap="go">再来一局</button>
        <button hidden="{{!plate}}" type="primary" class="result-btn b-sizing" data-type="share" bindtap="go">分享荣耀</button>
    </view>
    <view class="rule">
        规则：执法学徒、执法新兵，失败-0积分，获胜+1积分，平局不加不扣；执法骨干、执法先锋、执法专家，失败-1积分，获胜+1积分，平局不加不扣；
    </view>
</view>
