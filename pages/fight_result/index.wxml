<!--pages/fight_result/index.wxml-->
<view class="fight_result b-sizing">
    <view class="result pr df justBet ac">
        <view class="me">
            <view class="header">
                <image src="{{fightResult.homeHead}}"></image>
                <view 
                    wx:if="{{fightResult.homeScore>fightResult.awayScore}}"
                    class="crown pa animated fadeInDown ac"
                >
                    <image src="../../assets/images/fight_result/crown.png"></image>
                </view>
            </view>
            <view class="name overeli">{{fightResult.homeName}}</view>
            <view class="score fw600">{{fightResult.homeScore}}分</view>
            <view class="{{(fightResult.homeScore>=fightResult.awayScore?'':'error')+' integral fw600'}}">{{fightResult.homeResult}}</view>
        </view>
        <view class="vs">
            <image src="../../assets/images/fight_result/vs_res.png"></image>
        </view>
        <view class="rival pr">
            <view class="header">
                <image src="{{fightResult.awayHead}}"></image>
                <view 
                    wx:if="{{fightResult.homeScore<fightResult.awayScore}}" 
                    class="crown pa animated fadeInDown"
                >
                    <image src="../../assets/images/fight_result/crown.png"></image>
                </view>
            </view>
            <view class="name overeli">{{fightResult.awayName||"--"}}</view>
            <view class="score fw600">{{fightResult.awayScore}}分</view>
            <view class="{{(fightResult.homeScore<=fightResult.awayScore?'':'error')+' integral fw600'}}">{{fightResult.awayResult}}</view>
        </view>
        <view wx:if="{{plate}}" class="plate pa animated fadeInDown">
            <image src="{{plate}}"></image>
        </view>
    </view>
    <view class="btn-box df justBet">
        <button hidden="{{!plate}}" type="primary" class="result-btn b-sizing" data-type="regame" bindtap="go">再来一局</button>
        <button hidden="{{!plate}}" open-type="share" class="result-btn b-sizing">分享荣耀</button>
    </view>
    <view class="rule">
        规则：执法学徒、执法新兵，失败-0积分，获胜+1积分，平局不加不扣；执法骨干、执法先锋、执法专家，失败-1积分，获胜+1积分，平局不加不扣；
    </view>
</view>
